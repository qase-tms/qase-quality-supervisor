{
  "description": "Blocks Qase MCP tool calls that could delete data: any mcp__qase__*delete* tool call, and any mcp__qase__qase_api call whose method is DELETE — enforces the plugin's non-destructive design principle technically, not just in prompt text.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__qase__.*delete.*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/deny-destructive.sh"
          }
        ]
      },
      {
        "matcher": "mcp__qase__qase_api",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/deny-destructive-api.sh"
          }
        ]
      }
    ]
  }
}
